# Copy to .dev.vars for local `wrangler dev` testing (already gitignored).
# In production these are set on the Worker in the Cloudflare dashboard ->
# Settings -> Variables and Secrets, or synced by 0-sync-secrets.yml, not here.
BOT_TOKEN=123456:your-telegram-bot-token
WEBHOOK_SECRET=some-long-random-string
YOUTUBE_API_KEY=optional-youtube-data-api-v3-key
